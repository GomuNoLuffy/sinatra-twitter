<h2><%= @user.name %></h2>
<h3>@<%= @user.username %></h3>

<%Tweet.where(user_id: current_user.id).each do |tweet|%>
<p><a class="link" href="/tweets/<%= tweet.id %>"><%= tweet.title %></a> @<%= current_user.username %></p>
<% end %>